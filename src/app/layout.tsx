import type { Metadata } from "next"; import { Plus_Jakarta_Sans, Satisfy, DM_Serif_Display, Vibur, Outfit } from "next/font/google"; import "./globals.css"; import { VisualEditsMessenger } from "orchids-visual-edits"; import { Toaster } from "sonner"; import { Preloader } from "@/components/Preloader"; import { AnalyticsTracker } from "@/components/AnalyticsTracker";const plusJakartaSans = Plus_Jakarta_Sans({  subsets: ["latin"],  variable: "--font-plus-jakarta",  display: "swap",});const outfit = Outfit({  subsets: ["latin"],  variable: "--font-outfit",  display: "swap",});const satisfy = Satisfy({  weight: "400",  subsets: ["latin"],  variable: "--font-satisfy",  display: "swap",});const dmSerifDisplay = DM_Serif_Display({  weight: "400",  subsets: ["latin"],  variable: "--font-dm-serif",  display: "swap",});const vibur = Vibur({  weight: "400",  subsets: ["latin"],  variable: "--font-vibur",  display: "swap",});export const metadata: Metadata = {  title: "Just Smile Hospitality - Premier Catering in Delhi NCR & Noida",  description: "Just Smile Hospitality & Management Private Limited offers premium corporate, wedding, and event catering in Noida and Delhi NCR. Experience culinary excellence.",  keywords: "catering, hospitality, wedding catering, corporate catering, event management, food services, Delhi NCR, Noida catering, Just Smile Hospitality",  icons: {    icon: "/favicon.svg",  },};import { SmoothScrollProvider } from "@/components/providers/SmoothScrollProvider";export default function RootLayout({  children,}: Readonly<{  children: React.ReactNode;}>) {  return (    <html lang="en">      <body className={`${plusJakartaSans.variable} ${outfit.variable} ${satisfy.variable} ${dmSerifDisplay.variable} ${vibur.variable} antialiased`}>        <Preloader />        <AnalyticsTracker />        <SmoothScrollProvider>          {children}          <VisualEditsMessenger />          <Toaster />        </SmoothScrollProvider>      </body>    </html>  );}