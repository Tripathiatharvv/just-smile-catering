import { Header } from "@/components/Header";import { Footer } from "@/components/Footer";import { Gallery } from "@/components/Gallery";import { WhatsAppButton } from "@/components/WhatsAppButton";import { Metadata } from "next";export const metadata: Metadata = {  title: "Gallery | Just Smile Hospitality & Management Private Limited | Visual Portfolio",  description: "Take a visual tour of our culinary creations, elegant event setups, and successful corporate catering events. See why we're the preferred choice for premium hospitality.",};export default function GalleryPage() {  return (    <main className="min-h-screen bg-white">      <Header />      <div className="pt-20">        <Gallery />      </div>      <Footer />      <WhatsAppButton />    </main>  );}