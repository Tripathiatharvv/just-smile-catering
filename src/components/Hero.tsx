"use client";import { useRef } from "react";import Image from "next/image";import Link from "next/link";import { motion, useScroll, useTransform, useSpring } from "framer-motion";import { ArrowRight, Star, ConciergeBell, MoveDown } from "lucide-react";export function Hero() {  const containerRef = useRef<HTMLDivElement>(null);  const { scrollYProgress } = useScroll({    target: containerRef,    offset: ["start start", "end start"],  });  const y = useTransform(scrollYProgress, [0, 1], ["0%", "50%"]);  const opacity = useTransform(scrollYProgress, [0, 0.8], [1, 0]);  const scale = useTransform(scrollYProgress, [0, 1], [1, 1.1]);  const smoothY = useSpring(y, { stiffness: 100, damping: 30 });  return (    <section      ref={containerRef}      id="home"      className="relative h-[110vh] flex items-center justify-center overflow-hidden bg-[#0a0a0a]"    >      {}      <motion.div         style={{ y: smoothY, scale }}         className="absolute inset-0 z-0"      >        <Image          src="https://images.unsplash.com/photo-1555244162-803834f70033?w=1920&q=80"          alt="Luxury Catering Setup"          fill          priority          className="object-cover object-center opacity-90"        />        <div className="absolute inset-0 bg-gradient-to-b from-black/70 via-black/40 to-black/80" />        <div className="absolute inset-0 bg-[#5A1F1F]/20 mix-blend-overlay" />      </motion.div>      {}      <div className="absolute inset-0 overflow-hidden pointer-events-none z-10">        <motion.div            initial={{ opacity: 0, scale: 0.8 }}            animate={{ opacity: 1, scale: 1 }}            transition={{ duration: 1.5, ease: "easeOut" }}            className="absolute -top-20 -left-20 w-96 h-96 bg-[#D4A24C]/10 rounded-full blur-[100px]"        />        <motion.div            initial={{ opacity: 0, scale: 0.8 }}            animate={{ opacity: 1, scale: 1 }}            transition={{ duration: 1.5, ease: "easeOut", delay: 0.5 }}            className="absolute bottom-0 right-0 w-[500px] h-[500px] bg-[#5A1F1F]/20 rounded-full blur-[120px]"        />      </div>      {}      <motion.div         style={{ opacity }}        className="relative z-20 container mx-auto px-4 sm:px-6 lg:px-8 flex flex-col items-center text-center"      >        {}        <motion.div          initial={{ opacity: 0, y: -20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ duration: 0.8, delay: 0.2 }}          className="mb-8 inline-flex items-center gap-2 px-4 py-2 rounded-full border border-[#D4A24C]/30 bg-black/20 backdrop-blur-md"        >          <Star className="w-4 h-4 text-[#D4A24C] fill-[#D4A24C]" />          <span className="text-xs sm:text-sm font-sans tracking-[0.2em] text-white/90 uppercase">            Award Winning Hospitality          </span>        </motion.div>        {}        <h1 className="font-serif text-5xl sm:text-7xl lg:text-8xl text-white leading-[1.1] mb-8 max-w-5xl">          <motion.span            className="block"            initial={{ opacity: 0, y: 50 }}            animate={{ opacity: 1, y: 0 }}            transition={{ duration: 0.8, delay: 0.4, ease: [0.215, 0.61, 0.355, 1] }}          >            Curating          </motion.span>          <motion.span            className="block text-gradient relative"            initial={{ opacity: 0, y: 50 }}            animate={{ opacity: 1, y: 0 }}            transition={{ duration: 0.8, delay: 0.55, ease: [0.215, 0.61, 0.355, 1] }}          >            <span className="relative z-10 bg-gradient-to-r from-[#D4A24C] via-[#E6B65C] to-[#D4A24C] bg-clip-text text-transparent italic pr-2">              Unforgettable            </span>            <div className="absolute -bottom-2 sm:-bottom-4 left-0 w-full h-[2px] sm:h-[4px] bg-gradient-to-r from-transparent via-[#D4A24C] to-transparent opacity-50" />          </motion.span>          <motion.span            className="block"            initial={{ opacity: 0, y: 50 }}            animate={{ opacity: 1, y: 0 }}            transition={{ duration: 0.8, delay: 0.7, ease: [0.215, 0.61, 0.355, 1] }}          >            Moments          </motion.span>        </h1>        {}        <motion.p          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ duration: 0.8, delay: 0.9 }}          className="font-sans text-lg sm:text-xl text-gray-300 max-w-2xl mb-12 leading-relaxed font-light"        >          Elevate your events with our signature blend of exquisite culinary artistry and white-glove service.        </motion.p>        {}        <motion.div           initial={{ opacity: 0, y: 20 }}           animate={{ opacity: 1, y: 0 }}           transition={{ duration: 0.8, delay: 1.1 }}           className="flex flex-col sm:flex-row gap-6 items-center"        >          <Link            href="/menu"            className="group relative px-8 py-4 bg-[#D4A24C] text-[#1a1a1a] rounded-full font-sans font-semibold tracking-wide overflow-hidden transition-all hover:shadow-[0_0_40px_-10px_rgba(212,175,55,0.6)]"          >             <span className="relative z-10 flex items-center gap-2">               Explore Menus <ArrowRight className="w-5 h-5 transition-transform group-hover:translate-x-1" />             </span>             <div className="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out" />          </Link>          <Link            href="/contact"            className="group relative px-8 py-4 bg-transparent border border-white/30 text-white rounded-full font-sans font-medium hover:bg-white/10 transition-colors backdrop-blur-sm"          >             <span className="flex items-center gap-2">                Book Consultation <ConciergeBell className="w-5 h-5 group-hover:rotate-12 transition-transform" />             </span>          </Link>        </motion.div>      </motion.div>      {}      <motion.div        initial={{ opacity: 0 }}        animate={{ opacity: 1 }}        transition={{ delay: 1.5, duration: 1 }}        className="absolute bottom-10 left-1/2 -translate-x-1/2 z-20 flex flex-col items-center gap-2"      >        <span className="text-[10px] uppercase tracking-[0.3em] text-white/50">Scroll</span>        <div className="w-[1px] h-12 bg-gradient-to-b from-white/50 to-transparent">            <motion.div                animate={{ y: [0, 48, 0], opacity: [0, 1, 0] }}                transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}                className="w-full h-1/2 bg-[#D4A24C]"            />        </div>      </motion.div>    </section>  );}