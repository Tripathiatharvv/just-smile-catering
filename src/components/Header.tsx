"use client";import Link from "next/link";import { usePathname } from "next/navigation";import { useState, useEffect } from "react";import { motion, AnimatePresence } from "framer-motion";import { Menu as MenuIcon, X, Phone, ChevronDown, Utensils, Building2, Users, UtensilsCrossed } from "lucide-react";import { Logo } from "./Logo";const services = [  { name: "Event Catering", href: "/event-catering", icon: Utensils, description: "Weddings, parties & celebrations" },  { name: "Menu", href: "/menu", icon: UtensilsCrossed, description: "Explore our culinary offerings" },  { name: "Corporate Catering", href: "/corporate-catering", icon: Building2, description: "Professional business dining" },  { name: "Hospitality Manpower", href: "/hospitality-manpower", icon: Users, description: "Trained service professionals" },];const navLinks = [  { name: "Home", href: "/" },  { name: "About", href: "/about" },  { name: "Certificates", href: "/certificates" },  { name: "Contact", href: "/contact" },];export function Header() {  const pathname = usePathname();  const isHome = pathname === "/";  const [isScrolled, setIsScrolled] = useState(false);  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);  const [isServicesOpen, setIsServicesOpen] = useState(false);  const [isMobileServicesOpen, setIsMobileServicesOpen] = useState(false);  useEffect(() => {    const handleScroll = () => {      setIsScrolled(window.scrollY > 50);    };    window.addEventListener("scroll", handleScroll);    return () => window.removeEventListener("scroll", handleScroll);  }, []);  useEffect(() => {    if (isMobileMenuOpen) {      document.body.style.overflow = "hidden";    } else {      document.body.style.overflow = "unset";    }    return () => {      document.body.style.overflow = "unset";    };  }, [isMobileMenuOpen]);  const headerBgClass = isHome    ? isScrolled      ? "bg-[#CAA745] backdrop-blur-xl shadow-lg shadow-[#561C24]/5"      : "bg-transparent"    : "bg-[#CAA845] shadow-lg shadow-[#561C24]/5";  const textColorClass = isHome && !isScrolled ? "text-white" : "text-[#561C24]";  const hoverColorClass = isHome && !isScrolled ? "hover:text-[#D4A24C]" : "hover:text-white";  const activeLinkClass = isHome && !isScrolled ? "bg-gradient-to-r from-[#D4A24C] to-[#E6B65C]" : "bg-[#561C24]";  return (    <>      <motion.header        initial={{ y: -100 }}        animate={{ y: 0 }}        transition={{ duration: 0.6, ease: "easeOut" }}        className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${headerBgClass}`}      >        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">          <div className="flex items-center justify-between h-28">            <Link href="/" className="flex items-center gap-3 group">              <Logo className={textColorClass} />            </Link>            <nav className="hidden lg:flex items-center gap-8">              <Link                href="/"                className={`font-sans text-sm font-medium transition-colors relative group ${textColorClass} ${hoverColorClass}`}              >                Home                <span className={`absolute -bottom-1 left-0 w-0 h-0.5 transition-all duration-300 group-hover:w-full ${activeLinkClass}`} />              </Link>              <div                className="relative"                onMouseEnter={() => setIsServicesOpen(true)}                onMouseLeave={() => setIsServicesOpen(false)}              >                <button                  className={`flex items-center gap-1 font-sans text-sm font-medium transition-colors h-20 ${textColorClass} ${hoverColorClass} ${isServicesOpen ? (isHome && !isScrolled ? "text-[#D4A24C]" : "text-white") : ""}`}                >                  Our Services                  <ChevronDown                    size={16}                    className={`transition-transform duration-300 ${isServicesOpen ? "rotate-180" : ""                      }`}                  />                </button>                <AnimatePresence>                  {isServicesOpen && (                    <motion.div                      initial={{ opacity: 0, y: 10, scale: 0.95 }}                      animate={{ opacity: 1, y: 0, scale: 1 }}                      exit={{ opacity: 0, y: 10, scale: 0.95 }}                      transition={{ duration: 0.2 }}                      className="absolute top-full left-1/2 -translate-x-1/2 w-[500px] bg-white rounded-2xl shadow-2xl shadow-[#5A1F1F]/10 border border-[#D4A24C]/20 p-6 grid grid-cols-1 gap-2"                    >                      <div className="absolute -top-2 left-1/2 -translate-x-1/2 w-4 h-4 bg-white rotate-45 border-l border-t border-[#D4A24C]/20" />                      {services.map((service, index) => (                        <motion.div                          key={service.name}                          initial={{ opacity: 0, x: -10 }}                          animate={{ opacity: 1, x: 0 }}                          transition={{ delay: index * 0.05 }}                        >                          <Link                            href={service.href}                            className="flex items-center gap-4 p-4 rounded-xl hover:bg-gradient-to-r hover:from-[#5A1F1F]/5 hover:to-[#D4A24C]/5 transition-all group"                          >                            <div className="w-12 h-12 bg-gradient-to-br from-[#D4A24C]/20 to-[#5A1F1F]/10 rounded-xl flex items-center justify-center group-hover:from-[#D4A24C] group-hover:to-[#E6B65C] transition-all duration-300">                              <service.icon className="w-5 h-5 text-[#5A1F1F] group-hover:text-white transition-colors" />                            </div>                            <div>                              <div className="font-sans font-bold text-[#5A1F1F] group-hover:text-[#D4A24C] transition-colors">                                {service.name}                              </div>                              <div className="font-sans text-xs text-gray-500">                                {service.description}                              </div>                            </div>                          </Link>                        </motion.div>                      ))}                    </motion.div>                  )}                </AnimatePresence>              </div>              {navLinks.slice(1).map((link) => (                <Link                  key={link.name}                  href={link.href}                  className={`font-sans text-sm font-medium transition-colors relative group ${textColorClass} ${hoverColorClass}`}                >                  {link.name}                  <span className={`absolute -bottom-1 left-0 w-0 h-0.5 transition-all duration-300 group-hover:w-full ${activeLinkClass}`} />                </Link>              ))}            </nav>            <div className="flex items-center gap-4">              <Link                href="/contact"                className={`hidden md:flex items-center gap-2 px-6 py-3 rounded-full font-sans font-semibold text-sm transition-all hover:scale-105 ${isHome                  ? "bg-[#69272F] text-white hover:shadow-lg hover:shadow-[#69272F]/30"                  : "bg-[#561C24] text-white hover:shadow-lg hover:shadow-[#561C24]/30"                  }`}              >                <Phone size={16} />                Get Quote              </Link>              <button                onClick={() => setIsMobileMenuOpen(true)}                className={`lg:hidden p-2 transition-colors ${isHome                  ? "text-[#561C24] hover:text-[#CAA845]"                  : "text-[#561C24] hover:text-white"                  }`}              >                <MenuIcon size={24} />              </button>            </div>          </div>        </div>      </motion.header>      <AnimatePresence>        {isMobileMenuOpen && (          <motion.div            initial={{ opacity: 0 }}            animate={{ opacity: 1 }}            exit={{ opacity: 0 }}            className="fixed inset-0 z-50 lg:hidden"          >            <motion.div              initial={{ opacity: 0 }}              animate={{ opacity: 1 }}              exit={{ opacity: 0 }}              className="absolute inset-0 bg-[#561C24]/20 backdrop-blur-sm"              onClick={() => setIsMobileMenuOpen(false)}            />            <motion.div              initial={{ x: "100%" }}              animate={{ x: 0 }}              exit={{ x: "100%" }}              transition={{ type: "spring", damping: 25 }}              className="absolute right-0 top-0 bottom-0 w-80 bg-white shadow-2xl overflow-y-auto"            >              <div className="p-6">                <div className="flex items-center justify-between mb-8">                  <Logo className="text-[#561C24]" />                  <button                    onClick={() => setIsMobileMenuOpen(false)}                    className="p-2 text-[#561C24] hover:text-[#CAA845] transition-colors"                  >                    <X size={24} />                  </button>                </div>                <nav className="flex flex-col gap-2">                  <Link                    href="/"                    onClick={() => setIsMobileMenuOpen(false)}                    className="font-sans text-lg font-medium text-[#561C24] hover:text-[#CAA845] transition-colors py-3 border-b border-[#CAA845]/10"                  >                    Home                  </Link>                  <div className="border-b border-[#CAA845]/10">                    <button                      onClick={() => setIsMobileServicesOpen(!isMobileServicesOpen)}                      className="flex items-center justify-between w-full font-sans text-lg font-medium text-[#561C24] hover:text-[#CAA845] transition-colors py-3"                    >                      Our Services                      <ChevronDown                        size={20}                        className={`transition-transform duration-300 ${isMobileServicesOpen ? "rotate-180" : ""                          }`}                      />                    </button>                    <AnimatePresence>                      {isMobileServicesOpen && (                        <motion.div                          initial={{ height: 0, opacity: 0 }}                          animate={{ height: "auto", opacity: 1 }}                          exit={{ height: 0, opacity: 0 }}                          className="overflow-hidden pl-4"                        >                          {services.map((service) => (                            <Link                              key={service.name}                              href={service.href}                              onClick={() => setIsMobileMenuOpen(false)}                              className="flex items-center gap-3 py-3 text-gray-600 hover:text-[#CAA845] transition-colors"                            >                              <service.icon size={18} className="text-[#CAA845]" />                              <span className="font-sans font-medium">{service.name}</span>                            </Link>                          ))}                        </motion.div>                      )}                    </AnimatePresence>                  </div>                  {navLinks.slice(1).map((link) => (                    <Link                      key={link.name}                      href={link.href}                      onClick={() => setIsMobileMenuOpen(false)}                      className="font-sans text-lg font-medium text-[#561C24] hover:text-[#CAA845] transition-colors py-3 border-b border-[#CAA845]/10"                    >                      {link.name}                    </Link>                  ))}                </nav>                <Link                  href="/contact"                  onClick={() => setIsMobileMenuOpen(false)}                  className="flex items-center justify-center gap-2 bg-gradient-to-r from-[#CAA845] to-[#E6B65C] text-[#561C24] px-6 py-4 rounded-full font-sans font-semibold mt-8 w-full hover:shadow-lg hover:shadow-[#CAA845]/30 transition-all"                >                  <Phone size={16} />                  Get Quote                </Link>              </div>            </motion.div>          </motion.div>        )}      </AnimatePresence>    </>  );}